{
  "name": "my-app",
  "version": "0.1.0",
  "homepage": "https://paulepps.github.io/portfolio",
  "private": true,
  "scripts": {
      "predeploy": "npm run build",
      "deploy": "gh-pages -d build",
      "start": "react-scripts start",
      "build": "react-scripts build"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
